# Summary evidence research

For `${level}` period `${period}`, investigate `${query}` using llm_wiki Deep Chat.

Return only evidence that is new, attributable, and reusable for a later decision. Cite the source path for every claim. Do not use any `70_Summaries/` note as evidence; summaries are projections, never primary sources. Prefer Daily notes, ADRs, Cards, landed source files, and unresolved review items. The Data Hub owns selection, validation, and rendering; this response is contextual evidence only.
