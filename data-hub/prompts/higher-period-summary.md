# Structured Higher-Period Summary

Produce exactly one JSON object for `${level}` / `${period}` under `${contract_json}`, `${taxonomy_json}`, `${policy_json}` and `${evidence_json}`.

Synthesize across lower summaries without copying their bodies. Explain material period changes, durable decisions, risks, next focus, and knowledge evolution. Each item must cite supplied evidence groups and include required support IDs, lower-summary wikilinks, and period change. JSON only.
