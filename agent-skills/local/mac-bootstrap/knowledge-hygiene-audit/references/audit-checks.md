# Audit Checks

`hygiene_audit.py` reports:

- orphan candidates
- stale review items
- duplicate accepted ADR/Card candidates
- filename_date vs landing_date anomalies
- broken materialization paths or missing candidate markers

Use the report to decide which workflow needs repair next.
