# Review Boundary

The review queue exists between extraction and formal knowledge promotion.

- `accept` means the candidate may be materialized.
- `reject` means keep the ledger but do not promote it.
- `defer` means revisit later.
- `merge` means resolve into another accepted artifact first.

Use `generate_candidates.py` through this skill; do not edit SQLite directly unless repairing corruption.
